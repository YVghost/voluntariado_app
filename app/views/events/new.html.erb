<div class="max-w-lg">
  <h1 class="text-2xl font-bold text-slate-900 mb-6">Nuevo evento</h1>
  <%= render "form", event: @event %>
  <div class="mt-4">
    <%= link_to "← Volver", events_path, class: "text-sm text-slate-500 hover:text-slate-700 transition-colors" %>
  </div>
</div>
