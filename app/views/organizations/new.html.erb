<div class="max-w-lg">
  <h1 class="text-2xl font-bold text-slate-900 mb-6">Nueva organización</h1>
  <%= render "form", organization: @organization %>
  <div class="mt-4">
    <%= link_to "← Volver", organizations_path, class: "text-sm text-slate-500 hover:text-slate-700 transition-colors" %>
  </div>
</div>
