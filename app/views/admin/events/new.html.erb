<div class="max-w-5xl mx-auto">
  <div class="mb-6 flex items-center justify-between">
    <div>
      <p class="text-xs text-slate-400 mb-1">Panel de administración → Eventos</p>
      <h1 class="text-2xl font-bold text-slate-900">Nuevo evento</h1>
    </div>
    <%= link_to "← Volver al panel", admin_dashboard_index_path(tab: "eventos"),
          class: "text-sm text-slate-500 hover:text-slate-700 transition-colors shrink-0" %>
  </div>
  <%= render "events/form", event: @event %>
</div>
